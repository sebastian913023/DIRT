[build]
builder = "nixpacks"

[deploy]
startCommand = "echo '[container] started' && node -v && echo '[container] launching server' && node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/data"
